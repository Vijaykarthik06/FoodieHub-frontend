{
  "name": "foodiehub-frontend",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "18.x || 20.x",
    "npm": "9.x || 10.x"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.1",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build 2>/dev/null || react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "overrides": {
    "workbox-webpack-plugin": {
      "workbox-google-analytics": "6.6.0"
    }
  },
  "devDependencies": {
    "baseline-browser-mapping": "^2.9.18"
  }
}
